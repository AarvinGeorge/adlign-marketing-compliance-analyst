# project-context — private mirror of the planning docs and ground truths

Snapshot mirror (2026-07-13) of the marketing-compliance-checker project
artifacts that live OUTSIDE this app repo on Aarvin's machine: numbered
planning docs, CLAUDE.md orientation, DESIGN.md, ground-truth v1 (FROZEN,
460 records) and v2 (FROZEN, 367 records, train/test split), the gt2
judging harness, eval_v2 baseline harness + results, and trace-analysis.
Mirrored here because the parent directory's backup repo is PUBLIC and
deliberately excludes this client-sensitive project; this private repo is
the only safe remote. The DISK copies remain the working set — re-mirror
after major doc/dataset changes (rsync commands in the session log).
